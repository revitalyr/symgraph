# Symgraph Unified GUI

Единый интерфейс для индексирования и просмотра проектов кода.

## Возможности

- **Мультиязычная поддержка**: C++, Rust, Python, JavaScript/TypeScript
- **Автоматическое определение типа проекта**
- **Индексирование в реальном времени** с отображением прогресса
- **Веб-просмотрщик результатов** со встроенным поиском
- **Статистика базы данных** и навигация по файлам
- **Единый интерфейс** для всех операций

## Запуск

### Через Python
```bash
cd gui
python run_gui.py
```

### Прямой запуск
```bash
cd gui
python unified_symgraph_gui.py
```

## Структура

- `unified_symgraph_gui.py` - Основное GUI приложение
- `run_gui.py` - Простой запускной скрипт
- `README.md` - Этот файл

## Использование

### 1. Индексирование проекта

1. Перейдите на вкладку "📊 Index Project"
2. Выберите директорию проекта (кнопка "Browse..." или "Auto-Detect")
3. Система автоматически определит тип проекта
4. Настройте параметры при необходимости
5. Укажите путь к базе данных
6. Нажмите "🚀 Start Indexing"

### 2. Просмотр результатов

1. Перейдите на вкладку "👁️ View Results"
2. Выберите базу данных для просмотра
3. Нажмите "🌐 Open Web Viewer" для открытия веб-интерфейса
4. Или используйте "📊 Show Statistics" для детальной статистики

### 3. Настройки

1. Перейдите на вкладку "⚙️ Settings"
2. Настройте автоматическое открытие веб-просмотрщика
3. Очистите настройки при необходимости

## Поддерживаемые проекты

### C++ проекты
- Индикаторы: `CMakeLists.txt`, `Makefile`, `*.vcxproj`, `*.sln`
- Расширения: `.cpp`, `.cc`, `.cxx`, `.c`, `.h`, `.hpp`, `.hxx`
- Параметры: директория сборки, генератор CMake, путь к compile_commands.json

### Rust проекты
- Индикаторы: `Cargo.toml`
- Расширения: `.rs`
- Параметры: путь к манифесту, опциональный LSIF файл

### Python проекты
- Индикаторы: `requirements.txt`, `setup.py`, `pyproject.toml`
- Расширения: `.py`
- Параметры: точка входа

### JavaScript/TypeScript проекты
- Индикаторы: `package.json`, `tsconfig.json`
- Расширения: `.js`, `.ts`, `.jsx`, `.tsx`
- Параметры: точка входа

## Веб-просмотрщик

Веб-просмотрщик открывается на `http://localhost:5000` и предоставляет:

- **Статистику базы данных**: количество файлов, символов, связей
- **Просмотр файлов** по категориям с количеством символов
- **Поиск символов** по имени с фильтрацией
- **Интерактивный интерфейс** с обновлением в реальном времени

## Требования

- Python 3.7+
- tkinter (обычно включен в Python)
- Flask (для веб-просмотрщика, устанавливается автоматически)
- Работающий symgraph CLI инструмент

## Установка зависимостей

```bash
pip install flask
```

## Примечания

- GUI автоматически сохраняет настройки между запусками
- Веб-сервер запускается временно на порту 5000
- Все операции индексирования выполняются в фоновом потоке
- Поддерживается отмена операций индексирования
