
[package]
name = "symgraph-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
lsp-types = "0.94"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

# Shared models
symgraph-models = { path = "../symgraph-models" }
